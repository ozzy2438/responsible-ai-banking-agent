services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: banking
      POSTGRES_USER: banking_migration
      POSTGRES_PASSWORD: migration-local-only
    ports:
      - "55440:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U banking_migration -d banking"]
      interval: 2s
      timeout: 3s
      retries: 20
    volumes:
      - banking-data:/var/lib/postgresql/data

volumes:
  banking-data:
